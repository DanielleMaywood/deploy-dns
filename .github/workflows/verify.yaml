name: verify
on:
    push:
        branches:
            - main
jobs:
    verify-domains:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: ./scripts/verify.sh
